{% extends 'base.html.twig' %}

{% block title %}Hello ArticlesController!
{% endblock %}

{% block body %}
	<ul>
		{% for article in articles %}
		{# on fait une liste avec les chemin vers le controller et l'argument id #}
			<li><a href=" {{ path('app_get_article_by_id', {id : article.id} )}}">{{ article.title }}</a></li>
		{% endfor %}
	</ul>
{% endblock %}
